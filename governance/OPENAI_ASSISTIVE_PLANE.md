# OpenAI API Assistive Research and Data Engineering Contract

Status: active optional research plane. Governing plan: SHA-256 `651bbff29cb929cdc441178f67df59e87600a3bc8a54516a942562c7d09aa523`. Budget authority: USD 100 absolute maximum. All earlier model-routing and allocation tables are superseded.

## Authority boundary

The OpenAI API may accelerate evidence-backed extraction, entity candidate ranking, schema interpretation, quarantine classification, conflict reconciliation, source-route research, retrieval, development error analysis, and bounded review. It is not part of the deterministic forecast-critical path and is never a required runtime dependency.

Every result is untrusted candidate, review, quarantine, or rejected material until deterministic project validators accept it. OpenAI cannot directly write or control immutable raw captures, canonical entities, identity merges, PIT state, known-at timestamps, training features, labels, protected windows, model promotion, champion state, forecasts, BAS, Aggie Excess, or publication state. A model may extract only facts present in cited evidence. Missing or contradictory evidence must produce `UNKNOWN`, `NOT_PRESENT`, or `CONFLICT`; confidence is not a calibrated probability. Name-only entity merges and fabricated facts, timestamps, outcomes, statistics, metrics, or identities are forbidden.

## Controller and API contract

All calls flow through one governed local controller. New synchronous work uses the Responses API with `store: false`; non-urgent bulk work uses the Batch API and `/v1/responses`. Machine-consumed results use strict JSON Schema Structured Outputs. The controller owns credential discovery and redaction, task/model/reasoning routing, prompt and schema identity, budget admission, idempotency, retries, caching, content-addressed storage, provenance, deterministic validation, usage reporting, and cleanup. Scattered direct API calls are prohibited.

The controller reads only the nonempty `OPENAI_API_KEY` value from the authoritative repository `.env`; it never prints, logs, commits, copies, serializes, hashes as evidence, or sends that key as prompt content. Prompts are minimized to relevant cited excerpts. Credentials, cookies, authentication headers, private personal information, the whole data lake, large source trees, Git history, and protected evidence are excluded.

## Models and escalation

Deterministic parsing, matching, and validation run first. Validated routine bulk extraction, classification, tagging, missingness coding, and first-pass candidates start with `gpt-5-nano` at `minimal` or `low`, Batch first. `gpt-4o-mini` is a bounded task-specific non-reasoning/visual-layout A/B route only when it beats Nano on accepted-result cost or a required capability. Nano failures may escalate to `gpt-5.6-luna` at `none` or `low`; ambiguous entities, conflicting evidence, complex gamebooks, and multi-step reconciliation use `gpt-5.6-terra` at `low` or `medium`; `gpt-5.6-sol` at `medium` or `high` is reserved for gold-set challenge, the hardest adjudications, high-risk independent review, and genuinely difficult scientific or architectural analysis. Each escalation requires measured schema/evidence failure, ambiguity, review savings, risk reduction, or accepted-record value. Sol is never a blanket default. Embeddings require measured superiority over deterministic retrieval or deduplication. The optimization target is accepted, evidence-verified records per dollar.

## Budget and storage

The append-only local usage ledger reserves worst-case cost before a call and settles actual token cost after it. Maximum envelopes are: $3 controller/prompt/schema setup, $22 Nano Batch, $4 bounded 4o Mini A/B, $12 Luna harder high-volume work, $15 Terra complex analysis/reconciliation, $10 Sol gold/hard work, $2 embeddings, $10 cross-model QA, and a locked $22 value-gated reserve. Spend releases in $10, $30, $60, $90, and $100 cumulative stages backed respectively by initial-pilot, passing-pilot, measured accepted-record, second-value-review, and completion/contingency evidence. Alerts emit once at $10, $25, $50, $75, and $90; low-priority admission stops at $90. Terra may exceed its $15 base only through measured reserve release and can never exceed $25; Sol may exceed $10 only through measured reserve release and can never exceed $17. At least $5 remains locked for completion/contingency until the final stage. A job above $1 requires a recorded admission review. Settled plus outstanding reserved cost must never exceed the current stage or the absolute $100 cap.

Operational material lives outside Git under `C:\BatteredAggieSyndrome.data\openai\` in `requests`, `responses`, `batches`, `manifests`, `evals`, `quarantine`, `usage`, `runtime`, and `tmp`. Requests and responses are content-addressed. Manifests preserve Jira/task identity, source and capture hashes, prompt/schema/model/reasoning identity, estimated and actual usage/cost, evidence validation, output hash, and disposition. Batch output is downloaded, hashed, and validated locally; remote files are deleted after preservation when practical; abandoned local temporary files are cleaned.

## Evaluation and pilots

A local versioned evaluation corpus covers positive, negative, ambiguous, conflicting, schema-drift, PIT-sensitive, target-leakage, and abstention cases. It measures schema validity, field precision/recall, evidence accuracy, correct abstention, unsupported facts, entity top-k recall, false merges, repeat consistency, disagreement, cost per accepted record, review time saved, and quarantine rate. Hosted OpenAI Evals is not a project dependency.

Pilot A is historical gamebook-equivalent extraction. Pilot B assists unresolved entity review without replacing deterministic or human merge authority. Pilot C classifies quarantine and schema drift. Pilot D begins only when suitable timestamped injury/availability evidence exists. Each begins with a deterministic/human gold sample, includes meaningful representative Terra and Sol work before inexpensive routing is entrenched, remains in shadow/candidate mode, and receives empirical acceptance criteria before promotion. The completed A/B pilots already established Terra/Sol comparisons; Pilot C repeats meaningful hard-case coverage while adding Nano and any justified 4o Mini route. Routine validated formats move to Nano Batch; failures go to the cheapest empirically passing tier, Terra handles complex ambiguity, and only a small hard residue/high-risk challenge goes to Sol.

## Completion and nonclaims

Completion requires the controller, external storage, staged budget ledger, strict schemas, local evaluation harness, Nano/inexpensive-route comparison with bounded Luna and meaningful Terra/Sol references, representative Terra/Sol initial-pilot work, measured reserve releases, at least one useful bounded pilot, deterministic candidate validation, cleanup, and passing repository, provenance, Jira, secret, PIT, leakage, identity, and full-suite validation. API failure degrades to deterministic/local work and never globally blocks historical expansion. This integration does not establish historical completeness, production-model readiness, protected performance, A&M specialization lift, BAS, Aggie Excess, or any scientific result.
