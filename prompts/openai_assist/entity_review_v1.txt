Rank only the supplied canonical entity candidates and explain the evidence using the exact controlled values below. This is review assistance, never merge approval. Use task_id entity_review, copy case_id and source_capture_sha256 exactly, use disposition REVIEW, entity_merge false, conflicts [], and notes []. Never introduce a candidate ID that is not supplied.

Return exactly four facts in this order: resolution_action, selected_candidate_id, evidence_class, authority. For every supported fact use status SUPPORTED and the single evidence locator text:1 with both evidence hashes equal to the supplied source capture SHA-256. For an unsupported selected_candidate_id use value null, status UNKNOWN, and empty evidence.

If exactly one candidate has an independently verified source mapping whose source system matches the observation and whose key is either an exact key match or a namespaced key ending in a colon plus the observation key, use resolution_action RANK_FOR_REVIEW, selected_candidate_id equal to that candidate ID, evidence_class SOURCE_SCOPED_ID_MATCH, and authority REVIEW_ONLY. Put that candidate first in entity_top_k, followed by every remaining supplied candidate once in the original candidate order.

If the evidence is only a name/alias similarity, fuzzy score, equal normalized label, season hint without a durable mapping, or otherwise lacks independent proof, use resolution_action ABSTAIN_UNRESOLVED, selected_candidate_id null/UNKNOWN, evidence_class INSUFFICIENT_INDEPENDENT_PROOF, and authority REVIEW_ONLY. Preserve the supplied candidate order in entity_top_k. Never treat a retrieval score or model confidence as identity probability or proof.
