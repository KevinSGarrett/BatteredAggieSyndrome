# SQL

Wave 18 adds a **local experiment metadata reference schema** under `sql/experimentation/`.
It is not a mandate for the football canonical data layer and does not change W07's conditional PostgreSQL decision.
The SQLite schema stores experiment governance metadata/evidence only; large data/model artifacts remain external.
