# OpenRouter Assistive Development Plane

Status: governed foundation; paid execution disabled.

This contract adopts the user directive with SHA-256 `0A2087E4692977F14B96B0FEB9CD7F01A96A50D4F085E92FC83EAC192998CA89` and the mandatory plan with SHA-256 `5CC491A90A5DF11A066B88B665FE69C51B31BD8725185C2ED653B249B808F498`.

## Authority

OpenRouter is an optional, provider-neutral assistive development backend. It may produce patch candidates, independent reviews, reconciliation rankings, schema-drift interpretations, and visual/layout triage candidates. Every result is candidate, review, quarantine, or rejected evidence. Codex and deterministic project validators retain implementation, canonicalization, PIT, scientific, model-promotion, forecast, BAS, Aggie Excess, publication, Git, GitHub, and Jira authority.

The plane may not write canonical data, PIT state, training features, labels, protected evidence, champion state, forecasts, or publication state. It may not invent evidence, identities, known-at timestamps, outcomes, statistics, or completeness. Absent evidence remains `UNKNOWN`, `NOT_PRESENT`, or `CONFLICT`.

## Provider and budget separation

Direct OpenAI and OpenRouter remain separate providers behind the provider-neutral registry. One request uses one provider, one ledger, and one budget. The existing direct OpenAI USD 100 authorization does not transfer to OpenRouter.

The OpenRouter paid hard limit and released stage are both exactly USD 0.00. `PAID_OPENROUTER_BUDGET_NOT_AUTHORIZED` rejects every billable request locally before network dispatch. No proposed amount in the implementation plan is authorization. Public documentation and model-catalog capture are non-billable capability evidence.

## Privacy, routing, and output rules

The single OpenRouter backend loads `OPENROUTER_API_KEY` at runtime from the ignored authoritative root `.env`; it never prints, hashes, copies, serializes, or sends the credential as prompt content. Requests minimize cited excerpts and reject detected secrets.

The default provider policy is `require_parameters:true`, `data_collection:"deny"`, `zdr:true`, `allow_fallbacks:false`. Cross-model fallback is disabled. A route is not approved until its exact endpoint supports required parameters, strict schema, ZDR, usage/cost accounting, an authorized budget, and an empirical pilot. Provider cost missing from a response causes quarantine rather than a zero-cost assumption.

All machine-consumed results use strict JSON Schemas with every property required and `additionalProperties:false`. Request identity binds the Jira unit, base commit, source hashes, evidence excerpts, prompt/schema/policy versions, model, reasoning effort, authority, and output limit.

OpenRouter's current Batch Beta is not adopted by this foundation. It remains disabled until a separate privacy, ZDR, provider-retention, output-retention, schema, accounting, cleanup, and empirical-value gate passes. Direct OpenAI Batch remains governed by its existing independent contract.

## Storage and worker isolation

Operational payloads live outside Git under `C:\BatteredAggieSyndrome.data\assistive\openrouter` in `requests`, `responses`, `manifests`, `evals`, `quarantine`, `usage`, `runtime`, `tmp`, `worker_packets`, and `worker_results`.

Candidate patch workers must use an isolated external clean worktree based on a pinned commit. Packets contain only bounded relevant files and evidence. Patch paths must be relative, inside allowed roots, and must not traverse to `.env`, `.git`, external data, protected interfaces, or unrelated files. Codex reviews and applies accepted patches through normal validation and PR/Jira integration.

## Evaluation and adoption

`qwen/qwen3-coder-next` is only a capability candidate. It is not a paid, route-approved, or empirically adopted model. After the user separately authorizes a paid OpenRouter envelope, the initial bounded pilot must compare deterministic-only work with the candidate on a versioned gold corpus and measure strict schema validity, unsupported claims, evidence accuracy, patch applicability, test pass rate, review time saved, repeat consistency, cross-family disagreement, accepted value per dollar, and quarantine/rejection rates.

No model, provider, fallback, Batch route, or task format earns continuing use without passing its predeclared empirical gate. Negative results are preserved. Provider failure degrades to deterministic/local work and never globally blocks historical acquisition or modeling.

## Definition of Done

The non-billable foundation is complete only when the provider-neutral registry, single backend, USD 0 hard-stop ledger, schemas, redaction, content-addressed storage, worker packet/path controls, fake-client tests, official catalog capture, governance, Jira graph, and applicable repository/provenance/Jira/secret/full-suite validation pass through normal integration.

Paid pilots and continuing operations remain incomplete until explicit separate spending authorization and their empirical gates pass. Completion of this plane does not establish historical completeness, protected performance, production readiness, A&M lift, BAS, Aggie Excess, or any scientific result.
