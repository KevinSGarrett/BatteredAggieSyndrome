# Jira File-Manifest Validation

- Result: **PASS**
- Manifested files: **1738**
- Self-referential manifest/hash files intentionally excluded: **2**
- Errors: **0**

The manifest covers every other delivered file beneath `jira/`; validation fails on missing, extra, size-mismatched, or hash-mismatched content.
