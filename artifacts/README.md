# Artifact Policy

Large trained models, local MLflow stores, caches and experiment outputs are excluded from cumulative repository ZIPs. Later waves will define versioned artifact registry/retention conventions. Wave 01 contains no trained model artifacts.
