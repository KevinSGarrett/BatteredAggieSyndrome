# Local-only configuration template. Copy to .env if a future implementation needs it.
# NEVER commit .env or real credentials.
AGGIE_ANALYTICS_DATA_ROOT=<LOCAL_PATH_OUTSIDE_REPOSITORY>
CFBD_API_KEY=<OPTIONAL_LOCAL_SECRET>
