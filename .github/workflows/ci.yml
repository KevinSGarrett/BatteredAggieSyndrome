name: repository-ci

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  core-validation:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Repository integrity
        run: python tools/validate_repository.py --repo-root . --strict
      - name: Upgrade packaging tools
        run: python -m pip install --upgrade pip setuptools
      - name: Install dependency-free core
        run: python -m pip install -e .
      - name: Dependency policy
        run: python tools/validate_dependency_policy.py --repo-root .
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Runtime environment evidence
        run: python tools/capture_runtime_manifest.py --repo-root . --output runtime-environment.json
      - uses: actions/upload-artifact@v7
        with:
          name: runtime-environment-${{ matrix.os }}-py${{ matrix.python-version }}
          path: runtime-environment.json

  product-adapter:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install locked product adapter
        run: python -m pip install --require-hashes -r requirements/product.lock
      - name: Install local package without dependency resolution
        run: python -m pip install --no-deps -e .
      - name: Dependency consistency
        run: python -m pip check
      - name: Product license and notice inventory
        run: python tools/validate_product_supply_chain.py --repo-root . --check-installed --output product-supply-chain.json
      - name: Product and operations tests
        run: python -m unittest tests.test_w22_product_serving tests.test_w23_operations -v
      - name: W22 product boundary gate
        run: python tools/validate_w22_product.py --repo-root .
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: product-supply-chain-${{ runner.os }}
          path: product-supply-chain.json
          if-no-files-found: error

  security-policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Repository secret / forbidden / governance scan
        run: python tools/validate_repository.py --repo-root . --strict
      - name: Install core only
        run: python -m pip install -e .
      - name: Dependency pin/provenance policy
        run: python tools/validate_dependency_policy.py --repo-root .
      - name: Install pinned vulnerability auditor
        run: python -m pip install pip-audit==2.10.1
      - name: Audit hash-locked product dependencies
        run: python -m pip_audit -r requirements/product.lock --require-hashes --disable-pip --progress-spinner off
