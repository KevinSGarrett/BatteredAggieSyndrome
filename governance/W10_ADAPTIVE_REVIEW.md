# W10 Adaptive Review

## Conclusion
The planned feature-engineering/screening/lifecycle objective remains correct. The main correction is to distinguish **functional feature machinery** from **empirical feature promotion** because the full historical materialized matrices and protected walk-forward model evaluations do not yet exist.

## Changes adopted
- Treat every W09-permitted raw field as an EXPERIMENTAL candidate seed only.
- Use multiple transform windows/decays as experiment parameters; no universal rolling window is frozen.
- Require strict-prior semantics for lagged historical outputs.
- Use staged screening; no correlation/MI/importance method can promote alone.
- Require family ablation + temporal stability contracts before production promotion.
- Make lifecycle state target-specific and market-lane aware.
- Preserve rejection/demotion history and distinguish structural BANNED from empirical REJECTED.
- Add real deterministic reference code and synthetic tests, but record zero empirical promotions.

## Scope boundary
W11 team-state modeling and W17 protected empirical feature promotion are not executed in W10.
