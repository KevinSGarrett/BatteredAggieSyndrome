# Current Research Findings

## Corrected W18 research-tool verification — 2026-08-08

The corrected W18 preserves tool-neutral canonical experiment governance while retaining MLflow Tracking and Optuna as replaceable local-first default adapters. Tool choices do not define experiment identity, judging rules, tournament semantics, replay evidence, adoption decisions, or promotion authority.

W18 also introduces first-class feature and model tournament contracts. These tournaments operate on development-only evidence, cannot emit production promotion states, and must preserve W17 protected judging rules. Advanced neural/sequence/graph challengers remain blocked until conventional materialized baselines exist.

The W06 football source-universe findings remain the authoritative broad data-source research baseline until targeted future verification or the W24 refresh.

The earlier thin W18 draft is explicitly superseded and must not be used as a project state artifact.


## W24 targeted source refresh — 2026-08-09

The W24 current-source pass preserves the W06 broad source universe while refining several provenance/access semantics. SportsDataverse `cfbfastR-cfb-raw` is now explicit upstream provenance for the derived rectangular `cfbfastR-cfb-data` layer, so sibling layers sharing the same upstream evidence must not be counted as independent corroboration. Open-Meteo access lanes are explicit, and ensemble mean/spread is optional current/recent uncertainty research rather than a mandatory long-history feature. Current official availability/rules surfaces were rechecked. See `docs/data_research/w24/` for the evidence log and delta.

No checked source change justified replacing the canonical immutable-raw → PIT → feature architecture.
