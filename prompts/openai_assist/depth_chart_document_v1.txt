Extract only the requested depth-chart document facts that are directly stated in the supplied official Texas A&M game-notes page. Never guess, infer an injury or availability status, infer a historical publication timestamp, or use football knowledge outside the supplied page.

The case instruction identifies one position group. Keep the listed order and OR relationships within that group only. Preserve player names exactly as printed, including capitalization, apostrophes, diacritics, suffixes, and punctuation. Return jersey numbers as JSON integers. Do not merge identities, correct names, or infer that a player was available for any game merely because the player appears on a chart.

Every directly supported fact must use status SUPPORTED. The evidence locator is the fixed sentinel text:1 for every supported fact. Copy the supplied source capture SHA-256 into both evidence hash fields. If a requested fact is absent from the page, return value null, status NOT_PRESENT, and empty evidence. If the evidence state supplied by the case is explicitly unknown, return value null, status UNKNOWN, and empty evidence. If the page contradicts itself for a requested fact, return value null, status CONFLICT, and empty evidence.

For every case in this fixed pilot corpus, historical_publication_time_utc has the externally verified state UNKNOWN; return null with status UNKNOWN and empty evidence. The selected depth-chart excerpts contain no stated injury_status; return null with status NOT_PRESENT and empty evidence. These two deterministic metadata instructions do not authorize inferring a timestamp, injury, health, or availability fact.

Use task_id depth_chart_document_extraction, copy case_id and source_capture_sha256 exactly, use disposition CANDIDATE, entity_top_k [], entity_merge null, conflicts [], and notes ["PIT_TARGET_EXCLUDED"]. These results are shadow document-extraction candidates only and have no canonical, identity-merge, historical-known-at, PIT, training, protected-evaluation, availability, forecast, or publication authority.
